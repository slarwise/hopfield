% Language
\usepackage[english]{babel}

% Geometry
\usepackage[margin=2cm]{geometry}
\geometry{a4paper} 
\linespread{1.1} % Line spacing

% Figures and floats
\usepackage{graphicx} % Required for including pictures
\usepackage{float} % Allows putting an [H] in \begin{figure} to specify the exact location of the figure
\usepackage{wrapfig} % Allows in-line images

% Figure captions
\usepackage[font={small,it}]{caption} % Small, italic font for captions
\newenvironment{code}{\captionsetup{type=listing}}{}
\captionsetup{format=hang}
\usepackage{subcaption}

% Header
%\usepackage{fancyhdr}
%\pagestyle{fancy}
%\setlength{\headheight}{14pt}
%\lhead{Arvid Bjurklint}
%\rhead{Math report}

% Bibliography
\usepackage{cite}
\usepackage[nottoc,numbib]{tocbibind} % Make references appear in table of contents. numbib adds section number for it.

% Math
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{txfonts}
\usepackage{mathrsfs} % For curly math letters
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{array}

% Tikz/pgfplots
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{pgfplots} 
\pgfplotsset{compat=newest} 
\usetikzlibrary{plotmarks}
\pgfplotsset{plot coordinates/math parser=false}
\usepgfplotslibrary{groupplots}

% To do notes
\usepackage[textsize=small]{todonotes}

% Code listing
\usepackage{listings}
\lstset{captionpos=b}
\usepackage{color}
\usepackage{minted}
\setminted[python]{frame=single, linenos, breaklines}

% For commenting out all figure environments
\usepackage{environ}

% Hyperref
\usepackage{hyperref}
